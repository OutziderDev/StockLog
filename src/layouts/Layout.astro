---
import Sidebar from "../sections/Sidebar.astro";
---
<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
 		<title>StockLog</title>
	</head>
	<body>
    <Sidebar/>
    <main>
      <slot />
    </main>
	</body>
</html>

<style is:global>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  
  :root {
    --color-dark-light: #2f2f2f; 
    --color-morado-oscuro: #1c0e28;
    --color-morado-claro: #261636;
    --color-morado-hover: #2b1d3f;
    --color-naranja-claro: #e68429;/* FFA270; */
    --color-naranja-oscuro: #d56414; /* e48440 */
    --color-crema: #fff2e0;
  }

	body {
    background-color: var(--color-morado-oscuro);
    color: var(--color-crema);
    display: flex;
    min-height: 100dvh;
    position: relative;
	}

  main {
    flex: 1; 
  }

  /* Utilidades */
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    margin: -1px;
    padding: 0;
    border: 0;
    clip: rect(0 0 0 0);
    overflow: hidden;
    white-space: nowrap;
  }

  .container{
    padding: 3rem;
    display: flex;
    flex-direction: column;
    gap: 2rem;

    @media (width <= 1024px){
      padding: 2rem;
    }
  }

  .section-Header {
    width: 100%;
    display: flex;
    flex-direction: row;
    justify-content: space-between;

    @media ( width <= 768px){
      flex-direction: column;
      gap: 1rem;
    }

    & > h1 {
      font-size: 1.8rem;
      text-wrap: nowrap;
    }

    &  button {
      display: flex;
      align-items: center;
      gap:0.5rem;
      background-color: var(--color-naranja-oscuro);
      color: white;
      border: none;
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      cursor: pointer;
      font-size: 1rem;
      transition: background-color 0.3s ease;

      &:hover {
        background-color: var(--color-naranja-claro);
      }

      & > svg {
        stroke: var(--color-morado-oscuro);
        stroke-width: 3;
      }
    }
  }

  .inputbase {
    padding: 0.6rem;
    border-radius: 0.4rem;
    background-color: var(--color-morado-claro);
    color: var(--color-crema);
    outline: none;
    border: none;
    font-size: 1.2rem;

    &::placeholder{
      color: var(--color-crema);
      opacity: 0.6;
    }

    &:focus {
      outline: 1px solid var(--color-naranja-claro);
    }
  }

  .tablestyles {
    width: 100%;

    & > thead {
      background-color: var(--color-naranja-oscuro);
    }

    & th {
      padding: 0.5rem;
      text-transform: uppercase;
    }

    & td {
      padding: 0.5rem;
      font-size: 1.2rem;
      text-align: center;
      animation: fade-in 0.2s ease;
    }

    & tbody > tr:hover{
      background-color: var(--color-morado-hover);
    }

    & tbody tr:nth-child(even) {
      background: var(--color-morado-claro);
      
      &:hover{
        background-color: var(--color-morado-hover);
      }
    }
  }

  .formFilters {
    width: 100%;
    display: flex;
    justify-content: center;
    flex-direction: row;
    gap: 1rem;;

    @media ( width <= 768px) {
      flex-direction: column;
    }

    & > input[type="text"] {
      width: 50%;

      @media( width <= 768px) {
        width: 100%;
      }
    }

    & > #inputCategoria, #inputUbicacion {
      width: 25%;

      @media( width <= 768px) {
        width: 100%;
      }
    }

    & > .btnform {
      display: flex;
      align-items: center;
      gap: 0.3rem;
      padding: 0.3rem 1rem;
      border: none;
      border-radius: 0.4rem;
      cursor:pointer;
      background-color: var(--color-naranja-oscuro);
      color: white;
      &:hover {
        background-color: var(--color-naranja-claro);
      }
    }
  }

  @keyframes fade-in {
    0% { opacity: 0;}
    100% { opacity:1;}
  }
</style>
